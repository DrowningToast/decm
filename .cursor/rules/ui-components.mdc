---
globs: "apps/web/src/components/**/*.tsx"
description: "UI component patterns using Radix UI and Tailwind CSS"
---

# UI Components - Radix UI + Tailwind

## Component Pattern
Using **Radix UI** primitives with **Tailwind CSS** and **class-variance-authority**.

Base components in [apps/web/src/components/ui/](mdc:apps/web/src/components/ui/)

## Standard Pattern
```tsx
import { cva, type VariantProps } from "class-variance-authority"
import { cn } from "@/lib/utils"

const componentVariants = cva(
  "base-classes",
  {
    variants: {
      variant: { default: "default-styles" },
      size: { default: "default-size" },
    },
    defaultVariants: { variant: "default", size: "default" },
  }
)

function Component({ className, variant, size, ...props }: 
  React.ComponentProps<"element"> & 
  VariantProps<typeof componentVariants>
) {
  return (
    <element 
      className={cn(componentVariants({ variant, size, className }))}
      {...props} 
    />
  )
}
```

## Required Imports
```tsx
import * as React from "react"
import { cva, type VariantProps } from "class-variance-authority"
import { cn } from "@/lib/utils"
import { Slot } from "@radix-ui/react-slot"
```

## Guidelines
- Use `cn()` utility for className merging
- TypeScript interfaces extending `React.ComponentProps`
- `cva` for variant-based styling  
- Support `asChild` pattern with Radix Slot
- Export component and variants