# Lefthook configuration
# https://github.com/evilmartians/lefthook

pre-commit:
    parallel: true
    commands:
        lint-staged:
            run: pnpm exec lint-staged
            stage_fixed: true
